Use the Frontend persona (Aurelia).

Apply installed skills with emphasis on `vercel-react-best-practices`.

Task:
{{TASK}}

Constraints:
- keep diffs minimal and production-safe
- avoid speculative rewrites
- include validation steps and expected impact

Output:
1) summary
2) files changed
3) tests/verification run
4) follow-up recommendations
